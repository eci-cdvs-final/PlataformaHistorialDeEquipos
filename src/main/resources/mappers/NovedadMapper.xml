<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.cvds.persistence.mybatisimpl.mappers.NovedadMapper">

	<insert id="registarNovedad" parameterType="edu.cvds.entities.Novedad">
        insert into Novedad(id, elementoid, equipoid, fecha, titulo,usuarioid,detalle) values (#{id}, #{elementoid}, #{equipoid}, #{fecha},#{titulo}, #{usuarioid}, #{detalle})
    </insert>
	
	</mapper>